# Overlay Agent Dev-Klon
Separater Klon, NICHT die echte Instanz (/opt/overlay ist root-only).
Regel: niemals main/master pushen, niemals force-push.
Immer auf Branch overlay-agent/<name> pushen, Aaron reviewt und merged manuell.
